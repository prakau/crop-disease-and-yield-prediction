import tensorflow as tf

def build_model(input_shape, num_classes):
    # Define the architecture of the convolutional neural network
    # Use pre-trained models for transfer learning if applicable
    # Compile the model with appropriate loss function and optimizer
    # Return the compiled model
    pass

def train_model(model, train_data, train_labels, val_data, val_labels, epochs, batch_size):
    # Train the model on the training data
    # Evaluate the model on the validation data
    # Save the trained model weights
    # Return the trained model
    pass
